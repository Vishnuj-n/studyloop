Based on a deep architectural audit of this codebase, I have identified the following structural fragmentations, leaky layers, duplicate logic, and tangled dependency graphs corresponding to the 5 critical architectural failure modes outlined in the request.

Here is the executive audit table with findings and the simplest "Ponytail" fixes:

| Target File(s) & Line Ranges | Architecture Smell & Root Cause | Risk / Fragility Impact | The Simplest "Ponytail" Fix (Minimal LOC, Maximum Stability) NO PR |
| :--- | :--- | :--- | :--- |
| `internal/study/quiz_sync.go:582-585` | **1. Leaky DB Abstractions & Layer Inversion:** `SubmitQuizAttempt` performs a raw SQL `tx.QueryRow` on the `topics` table to check completion thresholds (`end_page`, `current_page_cursor`) instead of using a dedicated repository interface method. | High. Study service bypasses the `db` interface and executes raw SQL strings, directly coupling it to schema internals and invalidating interface boundaries. | Extract lines into a repository method `IsTopicFullyReadTx(tx *sql.Tx, topicID string) (bool, error)` in `internal/db/topics_repo.go` and call it from `quiz_sync.go`. |
| `internal/db/schema.go:33`, `internal/db/study_queue_queries.go:324` | **2. Fragmented Sources of Truth:** `current_page_cursor` exists on the `topics` table while reading progress logic relies heavily on the status of sequential `study_queue` entries (`start_page`/`end_page`). `GetReadingTask` explicitly joins and merges `sq.start_page` and `t.current_page_cursor`. | Moderate. Dual tracking of page progress in both `topics` and `study_queue` creates drift. The `study_queue` and its completion status should be the deterministic single source of truth for queue progress. | Rely entirely on active `study_queue` tasks to locate the student. For safety in MVP, stop updating `current_page_cursor` manually and instead make it a pure SQL `VIEW` based on completed `study_queue` chunks. |
| `internal/app/app_study_reading.go:201-221`, `internal/app/app_study_cards.go:606-608, 622-624` | **3. Scattered State Machines & Lifecycle Mutations:** UI API controllers directly orchestrate queue task completion (`CompleteReadingWithGeneratedQuiz`, `CompleteTask`) rather than deferring to a single unified queue router / state machine. | High. Task transition logic (e.g., ACTIVE -> COMPLETED and subsequent insertions) is duplicated across UI handlers, violating the architecture invariant of "explicit state mutations via single router". | Refactor UI controllers to pass a unified intent to a `study_queue` router layer (or a unified transition method in the `db` package) rather than handling multi-step manual completions themselves. |
| `internal/db/study_queue_repo.go:575-585`, `internal/app/app_study.go:205-209`, `internal/app/notebook_endpoints.go:858` | **4. Duplicate Pipelines & Split Hooks:** `EnsurePendingReadingTaskForNotebook` and `EnsurePendingReadingTasksForActiveNotebooks` are called from multiple unrelated entry points (endpoints, core app sync loops) creating divergent hooks. | Moderate. Unpredictable task insertion timing depending on which UI pathway the user takes, violating the deterministic queue progression invariant. | Centralize task initialization in a single boot/recovery hook or explicitly within the task completion lifecycle (`CompleteTaskTx`) of the preceding task, removing scattered hooks. |
| `internal/models/models.go:47-48`, `internal/app/app_study.go:301`, `internal/scheduler/service.go:216-224` | **5. Dead Tables, Over-Engineered Adapters & Speculative Code:** The `ReviewTaskDailyID` (`"task-review-daily"`) is a synthetic, in-memory orchestrated task ID that bypasses the actual SQLite `study_queue`. | Critical. Violates the core invariant: "No hidden orchestration... queue is only source of next actions. Deterministic progression is deterministically queue-driven via SQLite." | Remove the synthetic task logic entirely. Standard `FLASHCARD_REVIEW` tasks should be explicitly and durably inserted into the `study_queue` table by the FSRS scheduler when reviews are due. |
